name: 'Build'
description: 'Build the angular project for the specified stage'
inputs:
  stage:
    description: 'The stage to build for'
    required: true
    default: 'prod'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.stage }}
